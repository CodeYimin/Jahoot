import { ReactElement } from "react";

interface QuestionResultProps {
  score: number;
  correct: boolean;
}

function QuestionResult({}: QuestionResultProps): ReactElement {
  return <div></div>;
}

export default QuestionResult;
